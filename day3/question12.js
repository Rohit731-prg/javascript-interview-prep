// Trim leading and trailing spaces
// Input: " hello " → Output: "hello"

function trimStr(str) {
    return str.trim();
}

const inputString = " hello ";
const outputString = trimStr(inputString);
console.log(outputString);